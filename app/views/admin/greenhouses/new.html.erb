<div class="card">
  <div class="card-header card-header-success">
    <h4 class="card-title">
      <%= Greenhouse.human_title(:create) %>
      <% if policy(Greenhouse).index? %>
        <%= link_to_back admin_greenhouses_path%>
      <% end %>
    </h4>
  </div>
  <div class="card-body">
    <%= render 'form', greenhouse: @greenhouse %>
  </div>
</div>
