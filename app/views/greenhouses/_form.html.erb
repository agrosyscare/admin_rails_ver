<%= form_with(model: greenhouse) do |form| %>
  <% if greenhouse.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(greenhouse.errors.count, "error") %> prohibited this greenhouse from being saved:</h2>

      <ul>
        <% greenhouse.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
