<p id="notice"><%= notice %></p>

<h1>Greenhouses</h1>

<%= link_to 'New Greenhouse', new_greenhouse_path %>

<div class="card-body border border-primary rounded advanced_options mb-4 d-none">
</div>

<div class="table-responsive-sm">
  <table class="table" id="greenhouses-dttb" data-source="<%= datatable_greenhouses_path(format: :json) %>">
    <thead>
      <tr>
        <th data-data='id'>ID</th>
        <th data-data='name'>Name</th>
        <th data-data='description'>Description</th>
        <th data-data='buttons' data-orderable="false" data-class-name="all" data-searchable="false" class="skip-export"></th>
      </tr>
    </thead>
    <tbody>
    </tbody>
  </table>
</div>
